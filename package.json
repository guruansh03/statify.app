{
  "name": "statify",
  "version": "2.0.0",
  "private": true,
  "description": "Statify — Analyze your Spotify & YouTube Music data. Private, local, beautiful.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
